# Deploy only /admin as the Netlify site (admin-only surface)
[build]
  publish = "admin"
  functions = "netlify/functions"

# Make /admin resolve to the CMS root
[[redirects]]
  from = "/admin"
  to = "/"
  status = 200
